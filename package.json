{
  "name": "com.basmilius.jev",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc && bun run --cwd settings-ui build",
    "typecheck": "tsc --noEmit && bun run --cwd settings-ui typecheck",
    "test": "bun test"
  },
  "dependencies": {
    "@basmilius/homey-common": "^1.24.0"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.5",
    "@types/bun": "^1.3.14",
    "@types/homey": "npm:homey-apps-sdk-v3-types@^0.3.12",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
